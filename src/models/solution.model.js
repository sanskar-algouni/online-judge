const mongoose = require("mongoose");

//TODO complete the solution Schema with the following fields - ["language", "solution"]
/*
language - String, enum["Cpp", "Python"]
solution - String
*/

const solutionSchema = mongoose.Schema({});

module.exports.solutionSchema = solutionSchema;
